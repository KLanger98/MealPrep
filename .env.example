# Wrangler reads this file automatically. Copy to .env (gitignored) and paste a
# token for the personal Cloudflare account that owns the meal-prep worker.
# Create one at https://dash.cloudflare.com/profile/api-tokens using the
# "Edit Cloudflare Workers" template, then add D1 edit permission.
CLOUDFLARE_API_TOKEN=
