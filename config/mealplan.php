<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Meal Slots
    |--------------------------------------------------------------------------
    |
    | The rows shown on the calendar, in display order. Add or remove slots
    | here; existing assignments keep whatever slot string they were saved
    | with, so renaming a slot orphans its assignments — add, don't rename.
    |
    */

    'slots' => ['breakfast', 'lunch', 'dinner'],

];
